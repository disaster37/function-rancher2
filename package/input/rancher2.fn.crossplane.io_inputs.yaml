---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: inputs.rancher2.fn.crossplane.io
spec:
  group: rancher2.fn.crossplane.io
  names:
    categories:
    - crossplane
    kind: Input
    listKind: InputList
    plural: inputs
    singular: input
  scope: Namespaced
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: Input can be used to provide input to this Function.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          generateRancherProviderCredentials:
            description: To generate a provider credential with rancher provider,
              you can use this field to specify the provider credential to generate.
              If both RancherCredentialRef and GenerateRancherProviderCredential are
              specified, GenerateRancherProviderCredential will be used.
            items:
              description: ProviderCredentialRequest is used to specify the provider
                credential to generate with rancher provider.
              properties:
                authProvider:
                  default: local
                  description: The auth provider to use to generate the provider credential.
                  type: string
                name:
                  description: The provider credential name to create
                  type: string
                passwordSecretRef:
                  description: PasswordSecretRef is the reference to the secret key
                    that contains the password to authenticate to rancher.
                  properties:
                    key:
                      description: The key of the secret to select from.  Must be
                        a valid secret key.
                      type: string
                    name:
                      default: ""
                      description: |-
                        Name of the referent.
                        This field is effectively required, but due to backwards compatibility is
                        allowed to be empty. Instances of this type with an empty value here are
                        almost certainly wrong.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      type: string
                    optional:
                      description: Specify whether the Secret or its key must be defined
                      type: boolean
                  required:
                  - key
                  type: object
                  x-kubernetes-map-type: atomic
                ttl:
                  default: 0
                  description: The TTL when generate token for provider
                  format: int64
                  type: integer
                type:
                  default: ProviderConfig
                  description: The provider type to generate.
                  enum:
                  - ProviderConfig
                  - ClusterProviderConfig
                  type: string
                url:
                  description: The rancher URL to generate the provider credential
                    for.
                  type: string
                usernameSecretRef:
                  description: UsernameSecretRef is the reference to the secret key
                    that contains the username to authenticate to rancher.
                  properties:
                    key:
                      description: The key of the secret to select from.  Must be
                        a valid secret key.
                      type: string
                    name:
                      default: ""
                      description: |-
                        Name of the referent.
                        This field is effectively required, but due to backwards compatibility is
                        allowed to be empty. Instances of this type with an empty value here are
                        almost certainly wrong.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      type: string
                    optional:
                      description: Specify whether the Secret or its key must be defined
                      type: boolean
                  required:
                  - key
                  type: object
                  x-kubernetes-map-type: atomic
              required:
              - authProvider
              - name
              - passwordSecretRef
              - ttl
              - url
              - usernameSecretRef
              type: object
            type: array
          importProjects:
            description: To import project by name, you can use this field to specify
              the project name. If both projectName and projectID are specified, projectID
              will be used.
            items:
              description: Project is used to specify the project name and cluster
                name to search project ID. If both projectName and projectID are specified,
                projectID will be used.
              properties:
                clusterName:
                  description: The cluster name where the project is located
                  type: string
                name:
                  description: The project name
                  type: string
                providerConfigRef:
                  description: The provider config reference to observe rancher project
                  properties:
                    kind:
                      description: Kind of the referenced object.
                      type: string
                    name:
                      description: Name of the referenced object.
                      type: string
                  required:
                  - kind
                  - name
                  type: object
              required:
              - clusterName
              - name
              type: object
            type: array
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          rancherSecretRef:
            description: The rancher secret key that store the credentials
            properties:
              key:
                description: The key of the secret to select from.  Must be a valid
                  secret key.
                type: string
              name:
                default: ""
                description: |-
                  Name of the referent.
                  This field is effectively required, but due to backwards compatibility is
                  allowed to be empty. Instances of this type with an empty value here are
                  almost certainly wrong.
                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                type: string
              optional:
                description: Specify whether the Secret or its key must be defined
                type: boolean
            required:
            - key
            type: object
            x-kubernetes-map-type: atomic
        required:
        - rancherSecretRef
        type: object
    served: true
    storage: true
